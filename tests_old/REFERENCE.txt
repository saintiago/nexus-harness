Archived test reference. These files are excluded from active validation.
Their original imports and paths are intentionally preserved; they are not a runnable suite here.
Rebuild required behavior coverage in tests/ using docs/testing.md.
Suites whose decisions the rebuilt fast layer now carries were removed as that coverage was
rebuilt; what remains is the coverage that still belongs to another layer.

The rebuilt boundary layer now carries the process, Git, workspace and external-service adapter
contracts in tests/process.test.ts, tests/git.test.ts, tests/workspace.test.ts,
tests/workspace-continuation.test.ts, tests/jira-gateway.test.ts and tests/github-*.test.ts. The
suites it replaced were removed with it: git.test.ts, workspace.test.ts, workspace-branch.test.ts,
stop.test.ts and delivery.test.ts.

What stays here is the coverage those rebuilds did not carry: the coding-runtime adapter
(agent.test.ts), the configured setup/check round (checks.test.ts), the Jira connector's queue,
claim, pointer and publication behavior (jira.test.ts, queue-recovery.test.ts), the review scan's
GitHub reads (review-github.test.ts), the completion path (completion-github.test.ts), source
readiness (refresh.test.ts), reporting (report.test.ts), and the assembled CLI and queue
workflows. The fixture modules these files share (fixtures/boundary-operations.ts, lifecycle.ts,
local-target.ts, workspace.ts) stay with them as reference.
